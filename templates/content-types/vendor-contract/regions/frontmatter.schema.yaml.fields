  contract_vendor:
    type: string
    when: subtype == vendor
  contract_start:
    type: date
    format: YYYY-MM-DD
    when: subtype == vendor
  contract_end:
    type: date
    format: YYYY-MM-DD
    optional: true
    when: subtype == vendor
  contract_renewal_date:
    type: date
    format: YYYY-MM-DD
    optional: true
    when: subtype == vendor
  contract_amount:
    type: string
    optional: true
    when: subtype == vendor
  contract_owner:
    type: string
    when: subtype == vendor
  contract_terms_summary:
    type: list
    items: string
    optional: true
    when: subtype == vendor
