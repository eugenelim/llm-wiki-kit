  medical_record_person:
    type: string
    when: subtype == medical
  medical_record_date:
    type: date
    format: YYYY-MM-DD
    when: subtype == medical
  medical_record_provider:
    type: string
    optional: true
    when: subtype == medical
  medical_record_kind:
    type: string
    optional: true
    when: subtype == medical
