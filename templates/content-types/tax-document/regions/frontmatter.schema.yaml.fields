  tax_document_year:
    type: string
    when: subtype == tax
  tax_document_form:
    type: string
    when: subtype == tax
  tax_document_issuer:
    type: string
    when: subtype == tax
  tax_document_recipient:
    type: string
    optional: true
    when: subtype == tax
  tax_document_amount:
    type: string
    optional: true
    when: subtype == tax
