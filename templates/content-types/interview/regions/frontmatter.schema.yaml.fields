  interview_date:
    type: date
    format: YYYY-MM-DD
    when: subtype == interview
  interview_subject:
    type: string
    when: subtype == interview
  interview_purpose:
    type: string
    when: subtype == interview
  interview_questions:
    type: list
    items: string
    optional: true
    when: subtype == interview
  interview_findings:
    type: list
    items: string
    optional: true
    when: subtype == interview
  interview_follow_ups:
    type: list
    items: string
    optional: true
    when: subtype == interview
