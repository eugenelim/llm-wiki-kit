  receipt_vendor:
    type: string
    when: subtype == receipt
  receipt_date:
    type: date
    format: YYYY-MM-DD
    when: subtype == receipt
  receipt_amount:
    type: string
    when: subtype == receipt
  receipt_category:
    type: string
    optional: true
    when: subtype == receipt
