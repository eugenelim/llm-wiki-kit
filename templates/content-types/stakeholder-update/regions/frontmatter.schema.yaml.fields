  update_date:
    type: date
    format: YYYY-MM-DD
    when: subtype == stakeholder
  update_project:
    type: string
    when: subtype == stakeholder
  update_audience:
    type: list
    items: string
    when: subtype == stakeholder
  update_status:
    type: string
    when: subtype == stakeholder
  update_highlights:
    type: list
    items: string
    optional: true
    when: subtype == stakeholder
  update_risks:
    type: list
    items: string
    optional: true
    when: subtype == stakeholder
  update_asks:
    type: list
    items: string
    optional: true
    when: subtype == stakeholder
