# Bookmark Homepage — multi-view dashboard rendered by Obsidian Bases.
# Filters: only active bookmarks (type: bookmark, status: active).
# Add new bookmarks by creating type: bookmark pages in wiki/bookmarks/;
# they appear here automatically.

filters:
  and:
    - 'note.type == "bookmark"'
    - 'note.status == "active"'

views:
  - type: cards
    name: By Category
    groupBy:
      property: category
      direction: ASC
    order:
      - title
      - last_visited

  - type: table
    name: All Bookmarks
    order:
      - title
      - category
      - url
      - last_visited
      - tags

  - type: cards
    name: School Portals
    filters:
      and:
        - 'note.category == "school-portal"'
    order:
      - title

  - type: cards
    name: Vendor Logins
    filters:
      and:
        - 'note.category == "vendor-portal"'
    order:
      - title

  - type: cards
    name: Recipe Sources
    filters:
      and:
        - 'note.category == "recipe-source"'
    order:
      - title
